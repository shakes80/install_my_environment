#!/bin/bash
#nanc_lines=$(cat nanc.function)
#echo "$nanc_lines"

#awk -v addpattern="$nanc_lines" '/\# User configuration/{print $0 "\n" addpattern;next}1' ~/.zshrc

#sed -e '${startLineNum},${endLineNum}d' ~/.zshrc